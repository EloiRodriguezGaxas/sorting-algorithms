class Quicksort {
    constructor(unsorted) {
        this.unsorted = unsorted;
    }

    quicksort(low, high) {
        if(low<high) {
            this.partition((partitionIdx) => {

            })
        }
    }

    partition(low, high, callback) {
        
    }

}